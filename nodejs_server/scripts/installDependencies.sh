#!/bin/bash
# sudo su
BASEDIR=$(cd $(dirname $0); pwd)
cd ${BASEDIR}
cd ..
# sudo rm -rf node_modules
# sudo rm -rf package-lock.json 
# sudo rm -rf package.json 
# sudo rm -rf yarn.lock
# sudo npm cache clean -f

# # sudo npm install node-sass
# # sudo npm install node-gyp
# # sudo npm rebuild node-sass

# sudo npm install cron
# sudo npm install multer
# sudo npm install express
# sudo npm install cors
# sudo npm install express-fileupload
# sudo npm install sequelize
# sudo npm install dotenv
# sudo npm install sequelize-cli -g
# sudo npm install uuid
# sudo npm install shelljs
# sudo npm install express-session
# sudo npm install cas-authentication
# sudo npm install audiobuffer-to-wav --save
# sudo npm install xhr
# sudo npm install file-type
# sudo npm install waveheader
# sudo npm install short-uuid
# sudo npm install web-audio-api
# sudo npm install @google-cloud/speech
# sudo npm install wavefile
# sudo npm install -g env-cmd
# sudo npm install env-cmd
# #FOR MYSQL
# sudo npm install socket.io
# sudo npm install mysql
# sudo npm install mysql2
# sudo npm install --save mariadb
npm install